# Validated training stack: SGLang 0.5.13, CUDA 13.
#
# This is the default selected by `scripts/install.sh`. SGLang 0.5.13's own
# Transformers pin is inside verl's supported range: >=5.5.3, !=5.6.0, <5.11.
# Tested with Qwen3-VL, Qwen3.5, GLM-4.6V-Flash, and InternVL3.5 training paths.
#
#   bash scripts/install_sglang.sh
#
# Why the versions are what they are, where it is not obvious:
#
#   torch 2.11.0       what SGLang 0.5.13 pins.
#   transformers 5.8  what SGLang 0.5.13 pins; it also satisfies verl's range.
#   trl 0.9.6          verl's critic loads AutoModelForCausalLMWithValueHead from
#                      trl. It is installed without dependencies in the post file
#                      because its stale numpy<2 declaration conflicts with verl.
#   flashinfer 0.6.12  the pair SGLang 0.5.13 was built against. The Python and
#                      cubin packages must remain on the same version.

torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
torchao==0.17.0

sglang==0.5.13
sglang-kernel==0.4.3
flashinfer-python[cu13]==0.6.12
flashinfer-cubin==0.6.12
torch-memory-saver>=0.0.9.post1

# Qwen3.5's hybrid linear-attention layers. Without these, Transformers falls
# back to the reference PyTorch kernels, which work but make training impractical.
fla-core[cuda]==0.5.2
flash-linear-attention==0.5.2

transformers==5.8.1
tokenizers==0.22.2
accelerate==1.14.0
peft==0.19.1
# SGLang requires this for its model processors, including native-thinking VLMs.
mistral-common==1.11.7
# trl is NOT here -- see sglang-cu130-post.txt. Its numpy<2 bound contradicts
# verl's numpy>=2, which pip calls ResolutionImpossible.
safetensors==0.8.0
sentencepiece==0.2.1
qwen-vl-utils==0.0.14
datasets==5.0.0

ray==2.55.1
tensordict==0.10.0
torchdata==0.11.0
numpy==2.2.6
pyarrow==24.0.0
hydra-core==1.3.3
omegaconf==2.3.1
wandb==0.27.2
tensorboard==2.21.0
tyro==1.0.16
